package twenty_twenty_two;

import java.io.File;

public class ForRapidTesting {

    public static void main(String[] args) {
        // pull data as string from file
        // String data = Files.readString(Paths.get(".\\src\\AdventOfCode\\twenty_fifteen\\day_12\\data.txt"));

        //pull data by lines/symbols/etc. from file
//        String path = "data.txt";
//        File file = new File(path);

        //read from file using scanner
        // Scanner scanner = new Scanner(file);
//        Scanner scanner = new Scanner(file);
//        ArrayList<String> cleanedData = new ArrayList<>();
//        while (scanner.hasNextLine()){
//            cleanedData.add(scanner.nextLine());
//        }
//        scanner.close();
//        return cleanedData;
    }
}
